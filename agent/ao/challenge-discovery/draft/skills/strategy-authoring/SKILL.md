---
name: strategy-authoring
description: Author constrained TypeScript challenge strategies using only the ComicCrawler ctx API.
---

# Skill

Write `export const strategy = ...` with `detect`, `autoAttempt`, and `verifyReady`.

Only use ctx methods described in the task. Do not use browser globals or direct Playwright APIs.

