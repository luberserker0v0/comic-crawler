---
name: strategy-authoring
description: Author constrained TypeScript challenge strategies using only the provided strategy runtime methods.
---

# Skill

Write `export const strategy = ...` with `detect`, `autoAttempt`, and `verifyReady`.

Only use strategy runtime methods described in the task. Do not use browser globals or direct Playwright APIs.
